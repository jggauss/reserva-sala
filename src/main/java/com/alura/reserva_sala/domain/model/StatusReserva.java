package com.alura.reserva_sala.domain.model;

public enum StatusReserva {
    // Reserva válida e ocupando espaço na agenda
    ATIVA,
    // Reserva desconsiderada para cálculos de conflito
    CANCELADA

    // Getters
}